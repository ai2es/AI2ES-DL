trainable.custom\_layers.PCA
============================

.. currentmodule:: trainable.custom_layers

.. autoclass:: PCA
   :members:
   :show-inheritance:
   :inherited-members:

   
   .. automethod:: __init__

   
   .. rubric:: Methods

   .. autosummary::
   
      ~PCA.Q_init
      ~PCA.__init__
      ~PCA.add_loss
      ~PCA.add_metric
      ~PCA.add_update
      ~PCA.add_variable
      ~PCA.add_weight
      ~PCA.apply
      ~PCA.build
      ~PCA.call
      ~PCA.compute_mask
      ~PCA.compute_output_shape
      ~PCA.compute_output_signature
      ~PCA.count_params
      ~PCA.finalize_state
      ~PCA.from_config
      ~PCA.get_config
      ~PCA.get_input_at
      ~PCA.get_input_mask_at
      ~PCA.get_input_shape_at
      ~PCA.get_losses_for
      ~PCA.get_output_at
      ~PCA.get_output_mask_at
      ~PCA.get_output_shape_at
      ~PCA.get_updates_for
      ~PCA.get_weights
      ~PCA.set_weights
      ~PCA.streaming_tf_PCA
      ~PCA.with_name_scope
   
   

   
   
   .. rubric:: Attributes

   .. autosummary::
   
      ~PCA.activity_regularizer
      ~PCA.compute_dtype
      ~PCA.dtype
      ~PCA.dtype_policy
      ~PCA.dynamic
      ~PCA.inbound_nodes
      ~PCA.input
      ~PCA.input_mask
      ~PCA.input_shape
      ~PCA.input_spec
      ~PCA.losses
      ~PCA.metrics
      ~PCA.name
      ~PCA.name_scope
      ~PCA.non_trainable_variables
      ~PCA.non_trainable_weights
      ~PCA.outbound_nodes
      ~PCA.output
      ~PCA.output_mask
      ~PCA.output_shape
      ~PCA.stateful
      ~PCA.submodules
      ~PCA.supports_masking
      ~PCA.trainable
      ~PCA.trainable_variables
      ~PCA.trainable_weights
      ~PCA.updates
      ~PCA.variable_dtype
      ~PCA.variables
      ~PCA.weights
   
   